apiVersion: apps/v1
kind: Deployment
metadata:
  name: page-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: page-service
  template:
    metadata:
      labels:
        app: page-service
    spec:
      containers:
        - name: page-service
          image: yydwjj/todo-pageservice
          env:
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:mysql://mysql:3306/tododb?useSSL=false&serverTimezone=UTC"
            - name: SPRING_DATASOURCE_USERNAME
              value: "root"
            - name: SPRING_DATASOURCE_PASSWORD
              value: "123456"
            - name: SPRING_CLOUD_NACOS_DISCOVERY_SERVER_ADDR
              value: "nacos:8848"
          ports:
            - containerPort: 8083

---
apiVersion: v1
kind: Service
metadata:
  name: page-service
spec:
  selector:
    app: page-service
  ports:
    - protocol: TCP
      port: 8083
      targetPort: 8083